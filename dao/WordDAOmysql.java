package dao;

// 사용 안하는 클래스
public class WordDAOmysql {
	// WordDAO가 oracle용으로 만들어졌다고 가정 mysql로 변경한다면?
	// mysql용으로 이 클래스를 만들어서 다 새로 짜면, WordService에서 이름도 다 새로 맞춰서 해야함.
	// 새로운 이름도 다 찾아봐야한다. >> 불편. dao_inf로 새로 만든다.(인터페이스 활용)

}
